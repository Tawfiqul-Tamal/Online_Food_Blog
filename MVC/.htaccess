RewriteEngine On

# let real files (css, js, uploads, images) be served directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# everything else -> index.php?route=...
RewriteRule ^(.*)$ index.php?route=$1 [QSA,L]
